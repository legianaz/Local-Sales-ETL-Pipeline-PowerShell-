# Local-Sales-ETL-Pipeline-PowerShell-
A 6-stage ETL pipeline that turns a raw sales CSV into a self-contained HTML dashboard plus summary CSVs. Everything runs locally. No network calls, no external tools, no database. The source file is never modified.
